# QUESTIONS.md

実装中に発生した不明点・確認事項を記録する。

---

## 未決事項（デフォルト採用で進行）

以下は技術選定.md / Todo.md に記載の未決事項。実装をブロックしないため、デフォルト値で進行する。

- [x] DB選定 → **D1で開始**（CLAUDE.mdに記載済み）
- [x] 認証方式 → **JWTのみ**（MVPではシンプルに、refresh tokenは導入しない）
- [x] 公開RSSのprivate時挙動 → **404**（推奨通り、存在を秘匿）
- [x] R2公開URLの設計 → `{bucket}/audio/{assetId}.{ext}`, `{bucket}/images/{assetId}.{ext}`
- [x] 本番ドメイン → 環境変数 `BASE_URL` で設定可能にする
- [x] 管理者アカウント初期作成方法 → **シードデータ**で作成

---

## 実装中の質問

（発生次第追記）

